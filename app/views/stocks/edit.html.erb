<div class="card">
    <div class="card-header">
        <h1>Edit Stock Entry</h1>
    </div>

    <div class="card-body">
        <%= render 'form', stock: @stock %>

        <br/>

        <%= link_to 'Show', @stock, class: 'btn btn-success' %>
        <%= link_to 'Back', stocks_path, class: 'btn btn-secondary' %>
    </div>
</div>
