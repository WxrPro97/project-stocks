<div class="card">
    <div class="card-header">
        <h1>Add Stock</h1>
    </div>

    <div class="card-body">
        <%= render 'form', stock: @stock %>

        <br/>
        
        <%= link_to 'Back', stocks_path, class: 'btn btn-secondary' %>
    </div>
</div>